
# Functions References {#sec-functions}

# Functions Reference {#sec-packages-functions-reference}

This table consolidates the packages and commands used throughout the book, what each command does, and where it is first introduced.

```{r include=FALSE}
#| inclde: false
library(tibble)
library(dplyr)
library(knitr)
library(kableExtra)

ref_trying <- tribble(
  ~Package, ~Command, ~`What it does`, ~`First introduced`,
  "base R", "`<-`", "Assigns values to objects for later use.", "[Introduction to R](@intro-key-functions)",
  "base R", "`c()`", "Combines multiple values into a single vector.", "[Introduction to R](@intro-key-functions)",
  "base R", "`:`", "Creates integer sequences (e.g., `1:10`).", "[Introduction to R](@intro-key-functions)",
  "base R", "`[]`", "Indexes and subsets elements from vectors or data frames.", "[Introduction to R](@intro-key-functions)",
  "base R", "`$`", "Accesses or creates columns within a data frame.", "[Introduction to R](@intro-key-functions)",
  "base R", "`sqrt()`", "Computes square roots.", "[Introduction to R](@intro-key-functions)",
  "base R", "`log()` / `log10()`", "Computes natural and base-10 logarithms.", "[Introduction to R](@intro-key-functions)",
  "base R", "`round()`", "Rounds numeric values to a specified number of digits.", "[Introduction to R](@intro-key-functions)",
  "base R", "`class()`", "Identifies the data type (class) of an object.", "[Introduction to R](@intro-key-functions)",
  "base R", "`length()`", "Returns the number of elements in a vector.", "[Introduction to R](@intro-key-functions)",
  "base R", "`factor()`", "Converts character data into categorical (factor) variables.", "[Introduction to R](@intro-key-functions)",
  "base R", "`levels()`", "Displays the levels associated with a factor.", "[Introduction to R](@intro-key-functions)",
  "base R", "`data.frame()`", "Combines vectors into a tabular data structure.", "[Introduction to R](@intro-key-functions)",
  "base R", "`head()` / `tail()`", "Displays the first or last rows of a dataset.", "[Introduction to R](@intro-key-functions)",
  "base R", "`str()`", "Displays the internal structure and data types of a dataset.", "[Introduction to R](@intro-key-functions)",
  "base R", "`summary()`", "Produces descriptive summaries of variables or model results.", "[Introduction to R](@intro-key-functions)",
  "base R", "`table()`", "Creates frequency tables for categorical data.", "[Introduction to R](@intro-key-functions)",
  "base R", "`nrow()` / `ncol()`", "Returns the number of rows or columns in a dataset.", "[Introduction to R](@intro-key-functions)",
  "base R", "`colnames()`", "Displays or modifies column names of a data frame.", "[Introduction to R](@intro-key-functions)",
  "base R", "`read.csv()`", "Imports CSV files into R as data frames.", "[Introduction to R](@intro-key-functions)",
  "base R", "`getwd()` / `setwd()`", "Gets or sets the current working directory.", "[Introduction to R](@intro-key-functions)",
  "base R", "`install.packages()`", "Installs packages from CRAN.", "[Introduction to R](@intro-key-functions)",
  "base R", "`library()`", "Loads an installed package into the current R session.", "[Introduction to R](@intro-key-functions)",
  "base R", "`?function_name`", "Accesses built-in help documentation for a function.", "[Introduction to R](@intro-key-functions)",
  "base R", "`|>`", "Passes the result of one operation into the next.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "magrittr", "`%>%`", "Passes the result of one operation into the next.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`select()`", "Chooses specific columns from a dataset.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`filter()`", "Keeps rows that meet logical conditions.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`arrange()`", "Orders rows based on column values.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`mutate()`", "Creates or modifies columns.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`rename()`", "Renames columns using `new_name = old_name`.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`distinct()`", "Returns unique rows or value combinations.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`if_else()`", "Creates values based on a binary condition.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`case_when()`", "Applies multiple conditional rules.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "base R", "`is.na()`", "Identifies missing (`NA`) values.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "tidyr", "`drop_na()`", "Removes rows containing missing values.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`count()`", "Counts observations by group.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`group_by()`", "Groups data for grouped operations.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`summarise()`", "Computes summary statistics for groups.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`n()`", "Returns group size within `summarise()`.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "base R", "`sessionInfo()`", "Displays information about the current R session, including loaded packages.", "[Introduction to tidyverse](@tidyverse-keyfunctions)",
  "dplyr", "`inner_join()`", "Performs a SQL-style inner join, keeping only rows that match in both datasets.", "[Comparing Two Groups](@means-keyfunctions)",
  "tidyr", "`pivot_longer()`", "Converts data from wide format to long format.", "[Comparing Two Groups](@means-keyfunctions)",
  "tidyr", "`pivot_wider()`", "Converts data from long format back to wide format.", "[Comparing Two Groups](@means-keyfunctions)",
  "base R", "`rbind()`", "Combines multiple data frames by binding rows together.", "[Comparing Two Groups](@means-keyfunctions)",
  "base R", "`merge()`", "Joins two data frames together based on a shared key variable.", "[Comparing Two Groups](@means-keyfunctions)",
  "base R", "`mean()`", "Calculates the average of numeric values.", "[Comparing Two Groups](@means-keyfunctions)",
  "stats", "`t.test()`", "Tests whether two group means differ significantly.", "[Comparing Two Groups](@means-keyfunctions)",
  "stats", "`cor()`", "Computes Pearson correlation coefficients.", "[Correlation Analysis](@cor-keyfunctions)",
  "stats", "`cor.test()`", "Computes and tests correlations.", "[Correlation Analysis](@cor-keyfunctions)",
  "base R", "`pairs()`", "Creates a scatterplot matrix.", "[Correlation Analysis](@cor-keyfunctions)",
  "GGally", "`ggpairs()`", "Enhanced scatterplot matrix with correlations.", "[Correlation Analysis](@cor-keyfunctions)",
  "ppcor", "`pcor.test()`", "Computes partial correlations.", "[Correlation Analysis](@cor-keyfunctions)",
  "base R", "`ifelse()`", "Recodes variables conditionally.", "[Correlation Analysis](@cor-keyfunctions)",
  "base R", "`set.seed()`", "Ensures reproducibility when generating random data.", "[Comparing Multiple Means](@anova-keyfunctions)",
  "stats", "`rnorm()`", "Generates random values from a normal distribution.", "[Comparing Multiple Means](@anova-keyfunctions)",
  "stats", "`aov()`", "Fits ANOVA models.", "[Comparing Multiple Means](@anova-keyfunctions)",
  "supernova", "`supernova()`", "Displays ANOVA results in structured tables.", "[Comparing Multiple Means](@anova-keyfunctions)",
  "stats", "`TukeyHSD()`", "Performs post-hoc pairwise comparisons.", "[Comparing Multiple Means](@anova-keyfunctions)",
  "base R", "`plot()`", "Visualizes post-hoc comparison results.", "[Comparing Multiple Means](@anova-keyfunctions)",
  "base R", "`xtabs()`", "Constructs contingency tables using a formula interface.", "[Analyzing Categorical Data](@cat-analysis-keyfunctions)",
  "janitor", "`tabyl()`", "Creates clean contingency tables.", "[Analyzing Categorical Data](@cat-analysis-keyfunctions)",
  "janitor", "`adorn_percentages()`", "Converts counts to percentages.", "[Analyzing Categorical Data](@cat-analysis-keyfunctions)",
  "janitor", "`adorn_ns()`", "Displays counts and percentages together.", "[Analyzing Categorical Data](@cat-analysis-keyfunctions)",
    "janitor", "`clean_names()`", "Cleans names of an object.", "[Correlations](@cor-keyfunctions)",
  "stats", "`chisq.test()`", "Performs Chi-Square tests of independence.", "[Analyzing Categorical Data](@cat-analysis-keyfunctions)",
  "gmodels", "`CrossTable()`", "Detailed cross-tabulations.", "[Analyzing Categorical Data](@cat-analysis-keyfunctions)",
  "pheatmap", "`pheatmap()`", "Heatmap visualization of residuals or contributions.", "[Analyzing Categorical Data](@cat-analysis-keyfunctions)",
  "rcompanion", "`cramerV()`", "Measures association strength between categorical variables.", "[Analyzing Categorical Data](@cat-analysis-keyfunctions)",
  "stats", "`lm()`", "Fits linear regression models.", "[Linear Regression](@linear-reg-keyfunctions)",
  "broom", "`tidy()`", "Tidies model coefficients.", "[Linear Regression](@linear-reg-keyfunctions)",
  "broom", "`glance()`", "Extracts model-level statistics.", "[Linear Regression](@linear-reg-keyfunctions)",
  "lmtest", "`bptest()`", "Tests heteroscedasticity.", "[Linear Regression](@linear-reg-keyfunctions)",
  "stats", "`AIC()`", "Compares regression models.", "[Linear Regression](@linear-reg-keyfunctions)",
  "stats", "`step()`", "Performs stepwise model selection.", "[Linear Regression](@linear-reg-keyfunctions)",
  "stats", "`glm()`", "Fits generalized linear models, including logistic regression (binomial family).", "[Logistic Regression](@log-reg-keyfunctions)",
  "base R", "`exp()`", "Converts log-odds to odds ratios.", "[Logistic Regression](@log-reg-keyfunctions)",
  "caTools", "`sample.split()`", "Splits data into training/testing sets.", "[Logistic Regression](@log-reg-keyfunctions)",
  "caret", "`confusionMatrix()`", "Evaluates classification performance.", "[Logistic Regression](@log-reg-keyfunctions)",
  "pscl", "`pR2()`", "Computes pseudo R² values.", "[Logistic Regression](@log-reg-keyfunctions)",
  "caret", "`varImp()`", "Assesses predictor importance.", "[Logistic Regression](@log-reg-keyfunctions)",
  "car", "`vif()`", "Detects multicollinearity.", "[Logistic Regression](@log-reg-keyfunctions)",
  "pROC", "`roc()`", "Builds ROC curves.", "[Logistic Regression](@log-reg-keyfunctions)",
  "pROC", "`auc()`", "Computes area under the ROC curve (AUC).", "[Logistic Regression](@log-reg-keyfunctions)",
  "knitr", "`knitr::opts_chunk$set()`", "Sets global chunk options in R Markdown.", "[Reproducible Reporting](@r-markdown-keyfunctions)",
  "knitr", "`kable()`", "Creates formatted tables for reports.", "[Reproducible Reporting](@r-markdown-keyfunctions)",
  "nycOpenData", "`nyc_311()`", "Downloads NYC 311 Service Request data from NYC Open Data.", "[Introduction To tidyverse](@sec-loading-packages)", 
  "reproresearchR", "infection_treatments", "Infection treatments dataset.", "[Analyzing Categorical Data](@sec-intro-cat-analysis)",
    "reproresearchR", "exam_data", "Exam data dataset.", "[Correlation](@sec-intro-cor)",
  "reproresearchR", "pizza_prices", "Pizza prices dataset.", "[Linear Regression](@sec-linear-reg)"
)

library(stringr)

ref_trying <- ref_trying |>
  mutate(
    chapter_order = case_when(
      str_detect(`First introduced`, "@intro-key-functions") ~ 1,
      str_detect(`First introduced`, "@tidyverse-keyfunctions|@sec-loading-packages") ~ 2,
      str_detect(`First introduced`, "@sec-ggplot") ~ 3,
      str_detect(`First introduced`, "@means-keyfunctions") ~ 4,
      str_detect(`First introduced`, "@anova-keyfunctions") ~ 5,
      str_detect(`First introduced`, "@cat-analysis-keyfunctions") ~ 6,
      str_detect(`First introduced`, "@cor-keyfunctions") ~ 7,
      str_detect(`First introduced`, "@linear-reg-keyfunctions") ~ 8,
      str_detect(`First introduced`, "@log-reg-keyfunctions") ~ 9,
      str_detect(`First introduced`, "@r-markdown-keyfunctions") ~ 10,
      TRUE ~ 999
    )
  ) |>
  arrange(chapter_order, Package, Command) |>
  select(-chapter_order)
```

```{r}
#| echo: false
kable(ref_trying)
```
